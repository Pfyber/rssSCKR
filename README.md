# rssSCKR

A Python script to read RSS feeds from sckr.si.

## Usage

1. Install dependencies:
   ```
   pip install -r requirements.txt
   ```

2. Run the script:
   ```
   python rss_reader.py
   ```

This will fetch and display the feed titles and the first 5 entry titles from each RSS feed.